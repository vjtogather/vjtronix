import { z } from "zod";
export const productSchema = z.object({ name: z.string().trim().min(2).max(180), slug: z.string().trim().max(200).optional(), shortDescription: z.string().trim().min(5).max(500), description: z.string().min(1).max(200_000), sku: z.string().trim().min(2).max(100), price: z.coerce.number().nonnegative(), compareAtPrice: z.coerce.number().nonnegative().optional(), stock: z.coerce.number().int().nonnegative(), images: z.array(z.string().min(1)).min(1).max(10), galleryImages: z.array(z.string()).max(20), categoryId: z.string().optional(), featured: z.boolean(), status: z.enum(["DRAFT", "PUBLISHED", "ARCHIVED"]) });
export const productCategorySchema = z.object({ name: z.string().trim().min(2).max(80), slug: z.string().trim().max(100).optional(), description: z.string().trim().max(500).optional(), image: z.string().max(900_000).optional() });
