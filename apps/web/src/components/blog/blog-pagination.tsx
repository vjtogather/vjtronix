import Link from "next/link";
export function BlogPagination({ page, searchParams, totalPages }: { page: number; searchParams: Record<string, string | undefined>; totalPages: number }) { if (totalPages < 2) return null; const href = (next: number) => { const query = new URLSearchParams({ page: String(next) }); for (const [key, value] of Object.entries(searchParams)) if (value) query.set(key, value); return `/blog?${query}`; }; return <nav aria-label="Blog pagination" className="flex items-center justify-between border-t pt-6 text-sm"><span className="text-muted-foreground">Page {page} of {totalPages}</span><div className="flex gap-2">{page > 1 ? <Link className="rounded-lg border px-3 py-2 hover:bg-muted" href={href(page - 1)}>Previous</Link> : null}{page < totalPages ? <Link className="rounded-lg border px-3 py-2 hover:bg-muted" href={href(page + 1)}>Next</Link> : null}</div></nav>; }
