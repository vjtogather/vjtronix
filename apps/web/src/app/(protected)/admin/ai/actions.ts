"use server";
import { RoleName } from "@/generated/prisma/client"; import { requireRole } from "@/lib/auth/authorization"; import { prisma } from "@/lib/prisma"; import { revalidatePath } from "next/cache";
export async function saveAiConfiguration(form: FormData) { const s=await requireRole([RoleName.ADMIN,RoleName.SUPER_ADMIN]); await prisma.aiConfiguration.upsert({where:{id:"default"},create:{id:"default",welcomeMessage:String(form.get("welcomeMessage")||"Welcome! How can I help?"),systemPrompt:String(form.get("systemPrompt")||"")},update:{welcomeMessage:String(form.get("welcomeMessage")||""),systemPrompt:String(form.get("systemPrompt")||"")}});await prisma.auditLog.create({data:{actorId:s.user.id,event:"AI_CONFIGURATION_UPDATED"}});revalidatePath("/ai");revalidatePath("/admin/ai") }
export async function addAiFaq(form:FormData){const s=await requireRole([RoleName.ADMIN,RoleName.SUPER_ADMIN]);const question=String(form.get("question")||"").trim(),answer=String(form.get("answer")||"").trim();if(!question||!answer)return;await prisma.aiFaq.create({data:{question,answer}});await prisma.auditLog.create({data:{actorId:s.user.id,event:"AI_FAQ_CREATED",metadata:{question}}});revalidatePath("/ai");revalidatePath("/admin/ai")}
export async function addPromptTemplate(form:FormData){const s=await requireRole([RoleName.ADMIN,RoleName.SUPER_ADMIN]);const name=String(form.get("name")||"").trim(),prompt=String(form.get("prompt")||"").trim();if(!name||!prompt)return;await prisma.aiPromptTemplate.create({data:{name,prompt}});await prisma.auditLog.create({data:{actorId:s.user.id,event:"AI_PROMPT_TEMPLATE_CREATED",metadata:{name}}});revalidatePath("/admin/ai")}
