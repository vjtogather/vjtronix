import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { ProductEditor } from "@/components/admin/product-editor"; export default async function EditProduct({params}:{params:Promise<{id:string}>}){const [p,c]=await Promise.all([prisma.product.findUnique({where:{id:(await params).id}}),prisma.productCategory.findMany({select:{id:true,name:true}})]);if(!p)notFound();return <div className="p-5 sm:p-8"><h1 className="mb-6 text-3xl font-semibold">Edit product</h1><ProductEditor categories={c} product={{...p,categoryId:p.categoryId || "",price:Number(p.price),compareAtPrice:p.compareAtPrice?Number(p.compareAtPrice):undefined,images:p.images.join("\n"),galleryImages:p.galleryImages.join("\n")}}/></div>}
