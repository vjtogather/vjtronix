import Link from "next/link";
import { notFound } from "next/navigation";
import { BlogEditor } from "@/app/(protected)/admin/blog/blog-editor";
import { prisma } from "@/lib/prisma";
export default async function EditBlogPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [blog, categories, subCategories] = await Promise.all([prisma.blog.findUnique({ where: { id } }), prisma.category.findMany({ orderBy: [{ order: "asc" }, { name: "asc" }], select: { id: true, name: true } }), prisma.subCategory.findMany({ orderBy: [{ order: "asc" }, { name: "asc" }], select: { id: true, name: true, categoryId: true } })]); if (!blog) notFound(); const editorBlog = { ...blog, excerpt: blog.excerpt ?? undefined, thumbnail: blog.thumbnail ?? undefined, banner: blog.banner ?? undefined, seoTitle: blog.seoTitle ?? undefined, seoDescription: blog.seoDescription ?? undefined }; return <div className="space-y-6 p-5 sm:p-8 lg:p-10"><div><Link className="text-sm text-sky-700 dark:text-sky-300" href="/admin/blog">← Back to blogs</Link><h1 className="mt-4 text-3xl font-semibold">Edit blog</h1></div><BlogEditor blog={editorBlog} categories={categories} subCategories={subCategories} /></div>; }
