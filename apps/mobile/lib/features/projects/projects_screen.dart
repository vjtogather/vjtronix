import 'package:flutter/material.dart';
class ProjectsScreen extends StatelessWidget { const ProjectsScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Projects')), body: ListView(padding: const EdgeInsets.all(20), children: ['IoT monitor', 'Robotics platform'].map((x) => Card(child: ListTile(title: Text(x), subtitle: const Text('Flutter • ESP32 • PostgreSQL'), onTap: () {}))).toList())); }
class ProjectDetailScreen extends StatelessWidget { const ProjectDetailScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Project')), body: ListView(padding: const EdgeInsets.all(20), children: [const Placeholder(fallbackHeight: 220), const SizedBox(height: 16), Text('Project details', style: Theme.of(context).textTheme.headlineSmall), const Wrap(spacing: 8, children: [Chip(label: Text('Flutter')), Chip(label: Text('ESP32'))]), const SizedBox(height: 16), Row(children: [FilledButton.icon(onPressed: () {}, icon: const Icon(Icons.code), label: const Text('GitHub')), const SizedBox(width: 12), OutlinedButton.icon(onPressed: () {}, icon: const Icon(Icons.open_in_new), label: const Text('Live demo'))]) ])); }
