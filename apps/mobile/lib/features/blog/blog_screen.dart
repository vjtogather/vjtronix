import 'package:flutter/material.dart';
import 'package:flutter_markdown/flutter_markdown.dart';
class BlogScreen extends StatelessWidget { const BlogScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Blog')), body: ListView(padding: const EdgeInsets.all(20), children: [const SearchBar(hintText: 'Search articles'), const SizedBox(height: 12), Wrap(spacing: 8, children: ['All', 'Engineering', 'AI', 'Learning'].map((x) => FilterChip(label: Text(x), selected: x == 'All', onSelected: (_) {})).toList()), const SizedBox(height: 16), ...['Building reliable embedded systems', 'A practical guide to AI hardware'].map((x) => Card(child: ListTile(title: Text(x), subtitle: const Text('VJtronix • 5 min read'), trailing: IconButton(onPressed: () {}, icon: const Icon(Icons.bookmark_border)))))])); }
class BlogDetailScreen extends StatelessWidget { const BlogDetailScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(actions: [IconButton(onPressed: () {}, icon: const Icon(Icons.bookmark_border))]), body: const Markdown(data: '# Article\n\nReadable **Markdown** content is rendered here.\n\n```dart\nfinal project = VJtronix();\n```', padding: EdgeInsets.all(20))); }
